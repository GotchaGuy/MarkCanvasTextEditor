module.exports = {
    purge: ['./src/**/*.html', './src/**/*.vue', './src/**/*.jsx',], darkMode: false, // or 'media' or 'class'
    theme: {
        // extend: {},
        textColor: {
            'primary': '#50bf87',
        }
    },
    variants: {
        extend: {},
    },
    plugins: [],
}
